# Flow Agent 配置文件
# 设置 Anthropic API 密钥（优先级低于环境变量 ANTHROPIC_API_KEY）
anthropic_api_key: "your-api-key-here"

# 默认模型，agent() 未指定 model 时使用
default_model: "claude-sonnet-4-20250514"

# 默认超时（毫秒）
default_timeout: 120000