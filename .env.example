# Flow Agent 环境变量配置

复制此文件为 `.env` 并按需修改变量：

```bash
cp .env.example .env
```

## 变量说明

| 变量 | 必填 | 说明 |
|------|------|------|
| `ANTHROPIC_API_KEY` | 否 | Anthropic API Key，优先级高于 config.yaml |

## 优先级

`ANTHROPIC_API_KEY` 环境变量 > `.flow-agent/config.yaml` 中的 `anthropic_api_key` 字段。