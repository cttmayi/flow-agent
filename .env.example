# Flow Agent 环境变量配置

复制此文件为 `.env` 并按需修改变量：

```bash
cp .env.example .env
```

## 变量说明

| 变量 | 必填 | 说明 |
|------|------|------|
| `ANTHROPIC_API_KEY` | 否 | Anthropic API Key，可覆盖 config.yaml 中的设置 |

## 优先级

`.flow-agent/config.yaml` > 环境变量 > 占位密钥（代理模式）